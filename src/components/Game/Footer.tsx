import { Typography } from "@mui/material";
import React from "react";

const Footer = () => {
  return (
    <Typography variant="body2" className="opacity-50" fontSize={12}>
      Made with ❤️ by Federico Circosta
    </Typography>
  );
};

export default Footer;
